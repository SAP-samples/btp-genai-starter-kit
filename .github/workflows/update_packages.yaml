name: Update python packages

on:
  workflow_dispatch:

  # Schedule to run every Monday at 23:55
  schedule:
    - cron: '55 23 * * 1'

  workflow_call:

jobs:
  setup_infrastructure:
    name: Test
    runs-on: ubuntu-latest

    steps:
        - name: Check out Git repository
          id: checkout_repo
          uses: actions/checkout@v4

        - name: Setup python
          if: ${{ success() }}
          uses: actions/setup-python@v5
          with:
            python-version: '3.12' # install the python version needed
  
        - name: Update python package versions
          if: ${{ success() }}
          run: |
            cd config
            rm -rf requirements.txt
            python -m pip install pip-tools
            pip-compile

        - name: Commit and push changes
          run: |
            TARGET_BRANCH=$(date --rfc-3339=date)"
            git add -A
            git config user.email "btpgenaibot@users.noreply.github.com"
            git config user.name "[genai bot] package updates"
            git diff --quiet && git diff --staged --quiet || git commit -m "[genai bot] update python packages"
            git push origin ${{ TARGET_BRANCH }}
            echo "TARGET_BRANCH=$(TARGET_BRANCH)" >> ${GITHUB_ENV}
        
        - name: Create Pull Request
          uses: devops-infra/action-pull-request@v0.5.5
          with:
            github_token: ${{ secrets.GITHUB_TOKEN }}
            source_branch: ${{ TARGET_BRANCH }}
            target_branch: main
            title: Auto-update python packages
            body: "**Automated pull request**"
            label: enhancement
            new_string: "** Automatic pull request** to update python packages"
